using System;
using System.Collections.Generic;
using System.IO;
using System.Text;

namespace Compiler
{
    class Program
    {
        static void Main()
        {
            // InputOutput.Begin();
            // FileStream file = new FileStream("Lex.txt", FileMode.Create);
            // file.Close();
            InputOutput.Scan();

        }
    }
}